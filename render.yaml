services:
  - type: web
    name: pandemic-healthcare-stability-lab
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: waitress-serve --listen=0.0.0.0:$PORT pandemic_dashboard.app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
